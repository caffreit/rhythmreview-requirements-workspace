# Presenter walkthrough

Target length: 10 to 12 minutes.

1. Open **Sources** and select `SRC-001-P1` in the clinical discovery call.
2. Open **Discovery review** and load the saved replay extraction.
3. Reject `DC-004`, because the diagnostic claim conflicts with the agreed decision-support role.
4. Edit `DC-002` to express the underlying active-review need. Keep the saved reason.
5. On `DC-003`, record the human age decision as 22 and over.
6. On `DC-012`, defer retention to privacy and clinical governance. Do not supply a duration.
7. Apply the remaining fixture decisions and continue to **Requirements**.
8. Create replay drafts. Select `TMP-001` and split it into `REQ-004` and `REQ-005`.
9. Use precise handoff wording for `UN-004` and `REQ-004`, then run quality review.
10. Record a disposition for the retention ambiguity and the separately labelled AI-assisted diagnostic conflict. Resolve any remaining findings.
11. Submit as Alex Morgan. Switch to Jamie Chen and approve the candidate baseline.
12. Open `REQ-004` in **Baseline and audit**. Follow its parent, accepted discovery links, exact source passages, original proposal, and retained versions.
13. Reset the workspace for the next presentation.

The in-app **Start guided walkthrough** control keeps the same sequence visible during a demonstration.
